{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "dev2": "nuxt dev --https --ssl-cert localhost.pem --ssl-key localhost-key.pem",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "preview": "nuxt preview"
  },
  "devDependencies": {
    "nuxt": "3.0.0-rc.6",
    "sass": "^1.54.3",
    "sass-loader": "^13.0.2"
  },
  "dependencies": {
    "@nuxtjs/tailwindcss": "^5.3.0",
    "@storyblok/nuxt": "^4.0.1"
  }
}
